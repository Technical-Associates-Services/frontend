import styled from 'styled-components'

export const SeoContainer = styled.div`
  position: absolute;
  top: -10000em;
  width: 1px;
  height: 1px px;
  margin: -1px;
  padding: 0;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
`
